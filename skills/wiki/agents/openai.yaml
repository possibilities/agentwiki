interface:
  display_name: "Wiki"
  short_description: "Capture, find, and publish durable markdown with agentwiki"
  default_prompt: "Use $wiki to capture or find this in the document vault."
policy:
  allow_implicit_invocation: true
